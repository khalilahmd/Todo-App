const API_URL  = 'http://localhost:9000';

export const ADD_TODO = `${API_URL}/todos`;
export const GET_TODOS = `${API_URL}/todos`;
export const UPDATE_TODO = `${API_URL}/todos`;
export const ADD_SUBTASK = `${API_URL}/subtasks`;
export const UPDATE_SUBTASK = `${API_URL}/subtasks`;
export const UPDATE_ALL_SUBTASKS = `${API_URL}/subtasks/update-all`;